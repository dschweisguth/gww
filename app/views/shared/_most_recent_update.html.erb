The most recent update from Flickr began <%= @latest.created_at.getlocal.strftime("%A, %B %d, %H:%M %Z") %> <%= @latest.completed_at.nil? ? "and is still running. An update takes about six minutes." : "and completed at #{@latest.completed_at.getlocal.strftime("%A, %B %d, %H:%M %Z")}." %><br/>
