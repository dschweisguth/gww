The most recent update from Flickr began <%= @latest.created_at.getlocal.strftime("%A, %B %e, %k:%M %Z") %> <%= @latest.completed_at.nil? ? "and is still running. An update takes about 40 minutes." : "and completed at #{@latest.completed_at.getlocal.strftime("%A, %B %e, %k:%M %Z")}." %><br/>
