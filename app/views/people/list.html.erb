<h1><%= @people.length %> active members</h1>

<table>
<% @people.each do |person| %>
<tr>
  <td><%= person[:guesscount] %></td>
  <td><%= link_to person[:person].username, show_person_url(person[:person]) %> (<%= person[:photocount] %>)</td>
</tr>
<% end %>
</table>
