<div id="info-window">
  <%= thumbnail @post %>
  <p>
    Posted <%= @post.dateadded.strftime '%B %e, %Y' %>.<br/>
    <% guesses = @post.guesses; if ! guesses.empty? -%>
      Guessed by <%= guesses.map { |guess| "#{link_to_person guess.person}, #{guess.guessed_at.strftime '%B %e, %Y'}" }.join '; ' %>.
    <% end -%>
  </p>
</div>
