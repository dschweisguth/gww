<% content_for :title do %>Photos worth more than one point<% end %>
<%= render :partial => 'admin/photos/list',
  :locals => { :title => pluralize(@photos.length, 'photo') + ' worth more than one point' } %>
